Modify Inputs
=================


The primary input to **stitches** is the target data for matching in emulation with the package data. The quickstarter provides an example taking a time series of global average temperature values and processing with **stitches** functions for it to serve as target data. 